# Attention Note

Source type: synthetic note

Claims:

1. Attention computes a context-dependent weighted mixture over tokens.
2. Self-attention lets each token read information from the whole sequence in one layer.
3. Replacing recurrence with attention shortens communication paths and makes parallel training easier.
4. Attention by itself is order-agnostic, so positional information must be added separately.
5. Attention helps with long-range dependencies, but it should not be confused with durable memory or complete reasoning.
