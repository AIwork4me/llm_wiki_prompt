# Example Schema Note

这个样例 vault 默认使用仓库中的 [default profile](../../profiles/default/SCHEMA.md)。

这里保留的是一次最小 ingest 与 query-writeback 之后的产物快照，重点是展示：

- 页面如何作为真相源存在
- `index.md` 和 `log.md` 如何作为派生导航与审计文件存在
- `draft` 和 `self_check` 在实际文件中的样子
